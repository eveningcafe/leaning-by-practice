global:
  resolve_timeout: 1m

route:
  group_by: ['alertname']
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 1h
  receiver: 'web'

receivers:
  - name: 'web'
    # No external notifications - view alerts at http://localhost:9093
